apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: error-production
spec:
  template:
    spec:
      containers:
        - image: image
          ports:
            - containerPort: 9001
